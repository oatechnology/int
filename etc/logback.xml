<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} %thread %X{username} %-5level %logger - %message%n%ex</pattern>
		</encoder>
		<!--
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>INFO</level>
			<onMatch>ACCEPT</onMatch>
			<onMismatch>DENY</onMismatch>
		</filter>
		-->
	</appender>

	<logger name="ib.web.nexus" level="INFO" additivity="false">
		<appender-ref ref="STDOUT" />
	</logger>

	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>/home/users/pbarta/consumer.%d{yyyy-MM-dd}.log</fileNamePattern>
			<maxHistory>21</maxHistory>
		</rollingPolicy>
		<encoder>
         <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %-20class{0} : %message%n%ex</pattern>
		</encoder>
	</appender>

	<logger name="ibgroup.security.oauth.consumer.http.HttpConsumerClient" level="DEBUG" additivity="false">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE" />
	</logger>

	<root level="DEBUG">
		<appender-ref ref="STDOUT" />
		<appender-ref ref="FILE" />
	</root>

</configuration>
